seed: 42
model: distilbert-base-uncased
num_labels: 7
batch_size: 16
epochs: 3
learning_rate: 2e-5
data:
  dataset: neuralchemy/prompt-injection-Threat-Matrix
  config: multiclass
  processed_dir: data/processed
  train_file: train_augmented.parquet
output_dir: outputs/
mlflow:
  tracking_uri: file:./mlruns
  experiment_name: prompt-injection

